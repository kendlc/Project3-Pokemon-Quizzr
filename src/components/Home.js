import React from "react";
import Authentication from "./Authentication";

const Home = () => {

    return(
        <div>    
            <Authentication />
        </div>
    );
};

export default Home; 